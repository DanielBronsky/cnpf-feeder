## env.example
## Пример переменных окружения для локального запуска (без docker compose).
## Для docker compose обычно достаточно передать AUTH_SECRET при запуске.
MONGODB_URI="mongodb://localhost:27017/cnpf_feeder"
AUTH_SECRET="change_this_to_a_long_random_string"
GOOGLE_GEMINI_API_KEY="your_gemini_api_key_here"

## Сброс пароля (опционально)
## Если RESEND_API_KEY не задан — ссылка пишется в лог бэкенда (для разработки)
# RESEND_API_KEY="re_..."
# MAIL_FROM="CNPF Feeder <noreply@yourdomain.com>"
# FRONTEND_URL="http://localhost:3000"

